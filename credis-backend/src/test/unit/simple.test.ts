describe('Simple Test', () => {
  it('should pass', () => {
    expect(1 + 1).toBe(2);
  });
  
  it('should test basic operations', () => {
    expect(2 * 3).toBe(6);
    expect('hello'.toUpperCase()).toBe('HELLO');
  });
});
